class Solution {
public:
    void reverseString(vector<char>& s) {
        stack<char>st;
        for (char i :s){
            st.push(i);
        }
        for (int j = 0; j < s.size(); ++j) {
            s[j] = st.top();
            st.pop();
        }
    }
};
